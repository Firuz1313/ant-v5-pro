# =============================================================================
# ANT SUPPORT BACKEND - КОНФИГУРАЦИЯ ПЕРЕМЕННЫХ ОКРУЖЕНИЯ
# =============================================================================
# Скопируйте этот файл в .env и настройте под ваше окружение

# =============================================================================
# БАЗА ДАННЫХ
# =============================================================================

# --- ВАРИАНТ 1: NEON CLOUD DATABASE (РЕКОМЕНДУЕТСЯ) ---
# Получите строку подключения на https://neon.tech/
DATABASE_URL=postgresql://neondb_owner:your_password@ep-your-endpoint.us-east-2.aws.neon.tech/neondb?sslmode=require

# --- ВАРИАНТ 2: ЛОКАЛЬНАЯ POSTGRESQL ---
# Используйте эти настройки вместо DATABASE_URL для локальной БД
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=ant_support
# DB_USER=postgres
# DB_PASSWORD=your_local_password

# --- НАСТРОЙКИ ПОДКЛЮЧЕНИЯ ---
DB_SSL=true                        # true для облачных БД, false для локальных
DB_POOL_MIN=2                      # Минимальное количество соединений в пуле
DB_POOL_MAX=20                     # Максимальное количество соединений в пуле
DB_IDLE_TIMEOUT=30000              # Время простоя соединения (мс)
DB_CONNECTION_TIMEOUT=10000        # Таймаут подключения (мс)

# --- РЕЖИМ РАБОТЫ БД ---
USE_MOCK_DB=false                  # true = использовать моковые данные вместо реальной БД

# =============================================================================
# СЕРВЕР
# =============================================================================

NODE_ENV=development               # development | production | test
PORT=3000                          # Порт для backend сервера
DEBUG=true                         # Включить подробные логи для отладки

# =============================================================================
# БЕЗОПАСНОСТЬ
# =============================================================================

# --- JWT ТОКЕНЫ ---
JWT_SECRET=your-super-secret-jwt-key-change-in-production-to-64-char-random-string
JWT_EXPIRES_IN=7d                  # Время жизни JWT токенов

# --- ШИФРОВАНИЕ ПАРОЛЕЙ ---
BCRYPT_ROUNDS=12                   # Количество раундов для bcrypt (10-12 оптимально)

# =============================================================================
# CORS (Cross-Origin Resource Sharing)
# =============================================================================

# Разрешенные домены для CORS запросов (через запятую)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080,http://localhost:8081

# =============================================================================
# RATE LIMITING (Ограничение частоты запросов)
# =============================================================================

RATE_LIMIT_WINDOW_MS=900000        # Окно времени для подсчета запросов (15 минут)
RATE_LIMIT_MAX_REQUESTS=1000       # Максимальное количество запросов в окне (для разработки)

# Для продакшена рекомендуется:
# RATE_LIMIT_MAX_REQUESTS=100

# =============================================================================
# ЗАГРУЗКА ФАЙЛОВ
# =============================================================================

UPLOAD_MAX_SIZE=10485760           # Максимальный размер загружаемого файла (10MB)
UPLOAD_ALLOWED_TYPES=image/jpeg,image/png,image/gif,image/webp,image/svg+xml

# =============================================================================
# MIDDLEWARE
# =============================================================================

HELMET_ENABLED=true                # Включить security headers (Helmet.js)
COMPRESSION_ENABLED=true           # Включить gzip сжатие ответов
REQUEST_LOGGING=true               # Логировать HTTP запросы

# =============================================================================
# API КОНФИГУРАЦИЯ
# =============================================================================

API_VERSION=v1                     # Версия API
API_PREFIX=/api                    # Префикс для всех API роутов

# =============================================================================
# ЛОГИРОВАНИЕ И ОТЛАДКА
# =============================================================================

LOG_LEVEL=debug                    # error | warn | info | debug
REQUEST_TIMEOUT=30000              # Таймаут для HTTP запросов (мс)

# =============================================================================
# ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ
# =============================================================================

# --- НАСТРОЙКИ ДЛЯ РАЗРАБОТКИ ---
HOT_RELOAD=true                    # Автоперезагрузка при изменении файлов
PRETTY_LOGS=true                   # Красивое форматирование логов

# --- НАСТРОЙКИ ДЛЯ ПРОДАКШЕНА ---
# NODE_ENV=production
# DEBUG=false
# RATE_LIMIT_MAX_REQUESTS=100
# HELMET_ENABLED=true
# JWT_SECRET=super-secure-64-character-random-string-for-production-use-only

# =============================================================================
# ПРИМЕРЫ КОНФИ��УРАЦИЙ ДЛЯ РАЗНЫХ ОКРУЖЕНИЙ
# =============================================================================

# --- ЛОКАЛЬНАЯ РАЗРАБОТКА ---
# DATABASE_URL не задан, используем отдельные параметры:
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=ant_support
# DB_USER=postgres
# DB_PASSWORD=postgres
# DB_SSL=false
# NODE_ENV=development
# DEBUG=true

# --- NEON CLOUD (РАЗРАБОТКА) ---
# DATABASE_URL=postgresql://user:pass@host/db?sslmode=require
# DB_SSL=true
# NODE_ENV=development
# DEBUG=true

# --- PRODACTION (HEROKU/VERCEL/DIGITAL OCEAN) ---
# DATABASE_URL=postgresql://user:pass@prod-host/db?sslmode=require
# NODE_ENV=production
# DEBUG=false
# JWT_SECRET=your-production-secret-64-chars-long
# ALLOWED_ORIGINS=https://yourdomain.com,https://admin.yourdomain.com
# RATE_LIMIT_MAX_REQUESTS=100

# =============================================================================
# ИНСТРУКЦИИ ПО НАСТРОЙКЕ
# =============================================================================

# 1. СКОПИРУЙТЕ ЭТОТ ФАЙЛ:
#    cp .env.example .env

# 2. ВЫБЕРИТЕ ВАРИАНТ БАЗЫ ДАННЫХ:
#    - Для Neon: укажите DATABASE_URL и DB_SSL=true
#    - Для л��кальной: уберите DATABASE_URL, настройте DB_HOST, DB_PORT, etc.

# 3. НАСТРОЙТЕ БЕЗОПАСНОСТЬ:
#    - Смените JWT_SECRET на случайную строку
#    - Настройте ALLOWED_ORIGINS под ваши домены

# 4. ИНИЦИАЛИЗИРУЙТЕ БАЗУ ДАННЫХ:
#    npm run db:init

# 5. ЗАПУСТИТЕ СЕРВЕР:
#    npm run dev

# =============================================================================
# ПРОВЕРКА КОНФИГУРАЦИИ
# =============================================================================

# После настройки выполните:
# npm run db:check     # Проверка подключения к БД
# npm run db:stats     # Статистика таблиц
# npm run dev          # Запуск сервера

# Если все настроено правильно, вы увидите:
# ✅ Подключение к базе данных установлено
# 🚀 ANT Support API Server started successfully!
# 📍 Server running on 0.0.0.0:3000
